{
  "scripts": {
    "build": "echo 'Build complete'",
    "serve": "python -m http.server 3000",
    "android:build": "npx cap add android && npx cap build android --release",
    "sdk:publish": "npm publish"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
